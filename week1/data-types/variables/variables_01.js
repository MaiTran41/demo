let count = 0;

count++;

console.log(count);

/* 
Create a variable named ageLimit that cannot be re-assigned (use the correct keyword).
Assign the value 18 to ageLimit.
Use console.log() to print the value of ageLimit.
Use typeof to check and print the data type of ageLimit.
 */

const ageLimit = 18;

console.log(ageLimit);
console.log(typeof ageLimit);

/*
Create two variables: animalType and animalName.
Assign the value "Cat" to animalType and "Whiskers" to animalName.
Create a new variable named description and combine animalType and animalName into a sentence (e.g., "The Cat is named Whiskers.").
Use console.log() to print the value of description. 
*/

const animalType = "Cat";

const animalName = "Whisker";

// to do $, use option + 4
const description = animalName + "is named" + animalType;

console.log(description);
